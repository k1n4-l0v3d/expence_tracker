# AI Chat Assistant Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Добавить плавающий чат-виджет с ИИ-помощником (Groq + Llama3), который умеет добавлять/редактировать/удалять расходы и доходы через естественный язык.

**Architecture:** Flask-эндпоинт `/api/chat` принимает сообщение пользователя и историю диалога, формирует контекст (категории, баланс, последние записи), вызывает Groq API в JSON-режиме, парсит ответ и выполняет нужное действие в БД. Виджет — плавающая кнопка в правом нижнем углу, доступна на всех страницах через `base.html`.

**Tech Stack:** Python `groq` SDK, Llama3-8b-8192 (Groq), Vanilla JS, Bootstrap Icons (уже подключены).

---

### Task 1: Установить Groq SDK и добавить API-ключ

**Files:**
- Modify: `requirements.txt`
- Modify: `.env`

- [ ] **Step 1: Установить пакет в виртуальное окружение**

```bash
venv/bin/pip install groq==0.13.1
```

Ожидаемый вывод: `Successfully installed groq-0.13.1`

- [ ] **Step 2: Добавить groq в requirements.txt**

Открой `requirements.txt` и добавь строку после `openpyxl`:

```
groq==0.13.1
```

- [ ] **Step 3: Добавить GROQ_API_KEY в .env**

Открой `.env` и добавь строку:

```
GROQ_API_KEY=твой_ключ_от_groq_здесь
```

- [ ] **Step 4: Проверить установку**

```bash
venv/bin/python -c "from groq import Groq; print('groq OK')"
```

Ожидаемый вывод: `groq OK`

---

### Task 2: Добавить бэкенд — эндпоинт /api/chat

**Files:**
- Modify: `app.py` (вставить перед строкой `with app.app_context():`, то есть перед строкой 2331)

- [ ] **Step 1: Добавить импорт groq в начало app.py**

В начале файла, после строки `import openpyxl` (строка 19), добавь:

```python
import json
from groq import Groq
```

- [ ] **Step 2: Добавить вспомогательные функции и маршрут /api/chat**

Вставь следующий блок непосредственно перед строкой `with app.app_context():` (строка 2331). `_groq_client` инициализируется здесь — к этому моменту `load_dotenv()` уже вызван в начале файла:

```python
# ─── ИИ-помощник (Groq) ───────────────────────────────────────────────────────
_groq_client = Groq(api_key=os.getenv('GROQ_API_KEY')) if os.getenv('GROQ_API_KEY') else None

def _build_chat_context(user_id: int) -> dict:
    today = date.today()
    categories = Category.query.filter(
        Category.is_active.is_(True),
        db.or_(Category.user_id.is_(None), Category.user_id == user_id)
    ).order_by(Category.name).all()

    monthly_income = get_monthly_income(user_id, today.year, today.month)
    monthly_expenses = float(db.session.query(
        func.coalesce(func.sum(Expense.amount), 0)
    ).filter(
        Expense.user_id == user_id,
        extract('year',  Expense.expense_date) == today.year,
        extract('month', Expense.expense_date) == today.month,
        Expense.is_spent.is_(True),
        Expense.savings_account_id.is_(None),
    ).scalar())

    recent_expenses = (Expense.query
        .filter_by(user_id=user_id)
        .order_by(Expense.expense_date.desc(), Expense.id.desc())
        .limit(15).all())

    recent_income = (Income.query
        .filter_by(user_id=user_id)
        .order_by(Income.income_date.desc(), Income.id.desc())
        .limit(10).all())

    return {
        'today': today.isoformat(),
        'categories': categories,
        'monthly_income': monthly_income,
        'monthly_expenses': monthly_expenses,
        'remaining': monthly_income - monthly_expenses,
        'recent_expenses': recent_expenses,
        'recent_income': recent_income,
    }


def _build_system_prompt(ctx: dict) -> str:
    cat_names = ', '.join(c.name for c in ctx['categories'])

    exp_lines = '\n'.join(
        f"ID:{e.id} {float(e.amount):.0f}₽ [{e.category.name}] {e.expense_date}"
        + (f" {e.description}" if e.description else "")
        for e in ctx['recent_expenses']
    ) or 'нет данных'

    inc_lines = '\n'.join(
        f"ID:{i.id} {float(i.amount):.0f}₽ [{i.source}] {i.income_date}"
        + (f" {i.description}" if i.description else "")
        for i in ctx['recent_income']
    ) or 'нет данных'

    return f"""You are a financial assistant for a personal expense tracking app. Always respond ONLY in Russian.

CRITICAL: You MUST always respond with valid JSON and nothing else, in this exact format:
{{"message": "ответ пользователю на русском", "action": "название_действия", "params": {{}}}}

Available actions:
- add_expense: params: {{"amount": number, "category_name": "string", "description": "string (optional)", "date": "YYYY-MM-DD (optional, default today)"}}
- add_income: params: {{"amount": number, "source": "string", "description": "string (optional)", "date": "YYYY-MM-DD (optional, default today)"}}
- edit_expense: params: {{"expense_id": number, "amount": number (optional), "category_name": "string (optional)", "description": "string (optional)"}}
- delete_expense: params: {{"expense_id": number}}
- edit_income: params: {{"income_id": number, "amount": number (optional), "source": "string (optional)", "description": "string (optional)"}}
- delete_income: params: {{"income_id": number}}
- none: params: {{}} — use for showing data, greetings, questions, or when no DB change needed

USER CONTEXT:
Today: {ctx['today']}
Available categories: {cat_names}
This month: income {ctx['monthly_income']:.0f}₽, expenses {ctx['monthly_expenses']:.0f}₽, remaining {ctx['remaining']:.0f}₽

Recent expenses (use ID for edit/delete):
{exp_lines}

Recent income (use ID for edit/delete):
{inc_lines}

Rules:
1. Always match user's category to the closest name from the available categories list. Never invent new categories.
2. If asked to show data (balance, expense list, income list) — use action "none" and format the data from context in the message field.
3. Only include params actually specified by the user. Use today's date if no date given.
4. Always confirm what was done in the message field in Russian.
5. If category not found, use action "none" and list available categories in the message."""


def _find_category_by_name(name: str, categories: list) -> 'Category | None':
    name_l = name.lower().strip()
    for c in categories:
        if c.name.lower() == name_l:
            return c
    for c in categories:
        if name_l in c.name.lower() or c.name.lower() in name_l:
            return c
    return None


def _execute_chat_action(action: str, params: dict, user_id: int, ctx: dict) -> str | None:
    """Выполнить действие в БД. Возвращает сообщение об ошибке или None при успехе."""
    today = date.today()

    if action == 'add_expense':
        cat = _find_category_by_name(params.get('category_name', ''), ctx['categories'])
        if not cat:
            cat_names = ', '.join(c.name for c in ctx['categories'])
            return f"Категория «{params.get('category_name')}» не найдена. Доступные: {cat_names}"
        exp_date = today
        if params.get('date'):
            try:
                exp_date = date.fromisoformat(params['date'])
            except ValueError:
                pass
        exp = Expense(
            user_id=user_id,
            category_id=cat.id,
            amount=float(params['amount']),
            description=params.get('description') or None,
            expense_date=exp_date,
            is_planned=False,
            is_spent=True,
        )
        db.session.add(exp)
        db.session.commit()
        return None

    if action == 'add_income':
        inc_date = today
        if params.get('date'):
            try:
                inc_date = date.fromisoformat(params['date'])
            except ValueError:
                pass
        inc = Income(
            user_id=user_id,
            amount=float(params['amount']),
            source=params.get('source', 'Прочее').strip(),
            description=params.get('description') or None,
            income_date=inc_date,
        )
        db.session.add(inc)
        db.session.commit()
        return None

    if action == 'edit_expense':
        exp = Expense.query.filter_by(id=params.get('expense_id'), user_id=user_id).first()
        if not exp:
            return 'Расход не найден.'
        if params.get('amount'):
            exp.amount = float(params['amount'])
        if params.get('category_name'):
            cat = _find_category_by_name(params['category_name'], ctx['categories'])
            if cat:
                exp.category_id = cat.id
        if params.get('description'):
            exp.description = params['description']
        db.session.commit()
        return None

    if action == 'delete_expense':
        exp = Expense.query.filter_by(id=params.get('expense_id'), user_id=user_id).first()
        if not exp:
            return 'Расход не найден.'
        db.session.delete(exp)
        db.session.commit()
        return None

    if action == 'edit_income':
        inc = Income.query.filter_by(id=params.get('income_id'), user_id=user_id).first()
        if not inc:
            return 'Доход не найден.'
        if params.get('amount'):
            inc.amount = float(params['amount'])
        if params.get('source'):
            inc.source = params['source'].strip()
        if params.get('description'):
            inc.description = params['description']
        db.session.commit()
        return None

    if action == 'delete_income':
        inc = Income.query.filter_by(id=params.get('income_id'), user_id=user_id).first()
        if not inc:
            return 'Доход не найден.'
        db.session.delete(inc)
        db.session.commit()
        return None

    return None  # action == 'none' — только ответ, без изменений в БД


@app.route('/api/chat', methods=['POST'])
@login_required
@ban_check
def api_chat():
    if not _groq_client:
        return jsonify({'message': 'ИИ-помощник не настроен (отсутствует GROQ_API_KEY).', 'ok': False}), 503

    data = request.get_json(silent=True) or {}
    user_message = (data.get('message') or '').strip()
    history = data.get('history') or []

    if not user_message:
        return jsonify({'message': 'Пустое сообщение.', 'ok': False}), 400

    ctx = _build_chat_context(current_user.id)
    system_prompt = _build_system_prompt(ctx)

    messages = [{'role': 'system', 'content': system_prompt}]
    for h in history[-10:]:
        if h.get('role') in ('user', 'assistant') and h.get('content'):
            messages.append({'role': h['role'], 'content': str(h['content'])})
    messages.append({'role': 'user', 'content': user_message})

    try:
        response = _groq_client.chat.completions.create(
            model='llama3-8b-8192',
            messages=messages,
            response_format={'type': 'json_object'},
            temperature=0.3,
            max_tokens=512,
        )
        raw = response.choices[0].message.content
        parsed = json.loads(raw)
    except Exception:
        return jsonify({'message': 'ИИ-помощник недоступен, попробуй позже.', 'ok': False}), 503

    action  = parsed.get('action', 'none')
    params  = parsed.get('params') or {}
    message = parsed.get('message', '')

    error = _execute_chat_action(action, params, current_user.id, ctx)
    if error:
        return jsonify({'message': error, 'ok': False})

    return jsonify({'message': message, 'ok': True})
```

- [ ] **Step 3: Проверить что app.py запускается без ошибок**

```bash
source venv/bin/activate && python3 -c "import app; print('app OK')"
```

Ожидаемый вывод: `app OK`

---

### Task 3: Создать CSS для чат-виджета

**Files:**
- Create: `static/css/chat.css`

- [ ] **Step 1: Создать файл static/css/chat.css**

```css
/* ── Floating button ──────────────────────────────── */
#chat-toggle {
    position: fixed;
    bottom: 24px;
    right: 24px;
    width: 54px;
    height: 54px;
    border-radius: 50%;
    background: linear-gradient(135deg, #6366f1, #8b5cf6);
    border: none;
    color: white;
    font-size: 1.4rem;
    cursor: pointer;
    box-shadow: 0 4px 20px rgba(99,102,241,.45);
    z-index: 1050;
    transition: transform .2s, box-shadow .2s;
    display: flex;
    align-items: center;
    justify-content: center;
}
#chat-toggle:hover {
    transform: scale(1.08);
    box-shadow: 0 6px 28px rgba(99,102,241,.6);
}

/* ── Popup ────────────────────────────────────────── */
#chat-popup {
    position: fixed;
    bottom: 90px;
    right: 24px;
    width: 340px;
    max-height: 520px;
    background: var(--bs-body-bg);
    border: 1px solid var(--bs-border-color);
    border-radius: 16px;
    box-shadow: 0 8px 32px rgba(0,0,0,.2);
    display: flex;
    flex-direction: column;
    z-index: 1049;
    overflow: hidden;
    transform: translateY(20px);
    opacity: 0;
    pointer-events: none;
    transition: transform .25s ease, opacity .25s ease;
}
#chat-popup.open {
    transform: translateY(0);
    opacity: 1;
    pointer-events: all;
}

/* ── Header ───────────────────────────────────────── */
#chat-header {
    background: linear-gradient(135deg, #6366f1, #8b5cf6);
    padding: 12px 16px;
    display: flex;
    align-items: center;
    justify-content: space-between;
    color: white;
    flex-shrink: 0;
}
#chat-header .chat-title {
    display: flex;
    align-items: center;
    gap: 8px;
    font-weight: 600;
    font-size: .95rem;
}
#chat-header .chat-status { font-size: .72rem; opacity: .8; }
#chat-close {
    background: none;
    border: none;
    color: white;
    opacity: .8;
    cursor: pointer;
    font-size: 1.05rem;
    padding: 0;
    line-height: 1;
}
#chat-close:hover { opacity: 1; }

/* ── Messages ─────────────────────────────────────── */
#chat-messages {
    flex: 1;
    overflow-y: auto;
    padding: 12px;
    display: flex;
    flex-direction: column;
    gap: 8px;
    min-height: 200px;
}
.chat-bubble {
    max-width: 86%;
    padding: 8px 12px;
    border-radius: 12px;
    font-size: .875rem;
    line-height: 1.45;
    word-break: break-word;
    white-space: pre-wrap;
}
.chat-bubble.bot {
    background: var(--bs-secondary-bg);
    border-bottom-left-radius: 3px;
    align-self: flex-start;
}
.chat-bubble.user {
    background: #6366f1;
    color: white;
    border-bottom-right-radius: 3px;
    align-self: flex-end;
}
.chat-bubble.error {
    background: var(--bs-danger-bg-subtle);
    color: var(--bs-danger);
    border-bottom-left-radius: 3px;
    align-self: flex-start;
}

/* ── Typing indicator ─────────────────────────────── */
.chat-typing {
    display: flex;
    gap: 5px;
    align-items: center;
    padding: 10px 14px;
    background: var(--bs-secondary-bg);
    border-radius: 12px;
    border-bottom-left-radius: 3px;
    align-self: flex-start;
}
.chat-typing span {
    width: 7px;
    height: 7px;
    background: #6366f1;
    border-radius: 50%;
    animation: chat-dot 1.2s infinite ease-in-out;
}
.chat-typing span:nth-child(2) { animation-delay: .2s; }
.chat-typing span:nth-child(3) { animation-delay: .4s; }
@keyframes chat-dot {
    0%, 80%, 100% { transform: scale(.65); opacity: .5; }
    40%            { transform: scale(1);   opacity: 1; }
}

/* ── Input area ───────────────────────────────────── */
#chat-input-area {
    padding: 8px;
    border-top: 1px solid var(--bs-border-color);
    display: flex;
    gap: 6px;
    align-items: flex-end;
    flex-shrink: 0;
}
#chat-input {
    flex: 1;
    border: 1px solid var(--bs-border-color);
    border-radius: 10px;
    padding: 8px 12px;
    font-size: .875rem;
    resize: none;
    max-height: 100px;
    background: var(--bs-body-bg);
    color: var(--bs-body-color);
    outline: none;
    font-family: inherit;
}
#chat-input:focus { border-color: #6366f1; }
#chat-send {
    width: 36px;
    height: 36px;
    border-radius: 10px;
    background: #6366f1;
    border: none;
    color: white;
    cursor: pointer;
    display: flex;
    align-items: center;
    justify-content: center;
    font-size: 1rem;
    flex-shrink: 0;
    transition: background .15s;
}
#chat-send:hover   { background: #4f46e5; }
#chat-send:disabled { background: #94a3b8; cursor: not-allowed; }

@media (max-width: 400px) {
    #chat-popup  { width: calc(100vw - 16px); right: 8px; bottom: 80px; }
    #chat-toggle { bottom: 16px; right: 16px; }
}
```

---

### Task 4: Создать JavaScript для чат-виджета

**Files:**
- Create: `static/js/chat.js`

- [ ] **Step 1: Создать файл static/js/chat.js**

```javascript
(function () {
    'use strict';

    const history = [];

    function csrf() {
        return document.querySelector('meta[name="csrf-token"]')?.content ?? '';
    }

    function buildWidget() {
        /* Floating button */
        const btn = document.createElement('button');
        btn.id = 'chat-toggle';
        btn.title = 'ИИ-помощник';
        btn.innerHTML = '<i class="bi bi-robot"></i>';

        /* Popup */
        const popup = document.createElement('div');
        popup.id = 'chat-popup';
        popup.innerHTML = `
<div id="chat-header">
  <div class="chat-title">
    <i class="bi bi-robot"></i>
    <div>
      <div>ИИ-помощник</div>
      <div class="chat-status">● Groq / Llama3</div>
    </div>
  </div>
  <button id="chat-close" title="Закрыть"><i class="bi bi-x-lg"></i></button>
</div>
<div id="chat-messages">
  <div class="chat-bubble bot">Привет! Я помогу управлять финансами. Попробуй написать: <em>«добавь расход 500 рублей на кафе»</em></div>
</div>
<div id="chat-input-area">
  <textarea id="chat-input" rows="1" placeholder="Напиши сообщение..."></textarea>
  <button id="chat-send" title="Отправить"><i class="bi bi-send-fill"></i></button>
</div>`;

        document.body.appendChild(btn);
        document.body.appendChild(popup);

        btn.addEventListener('click', () => popup.classList.toggle('open'));
        document.getElementById('chat-close').addEventListener('click', () => {
            popup.classList.remove('open');
        });

        const input   = document.getElementById('chat-input');
        const sendBtn = document.getElementById('chat-send');

        input.addEventListener('keydown', (e) => {
            if (e.key === 'Enter' && !e.shiftKey) { e.preventDefault(); send(); }
        });
        input.addEventListener('input', () => {
            input.style.height = 'auto';
            input.style.height = Math.min(input.scrollHeight, 100) + 'px';
        });
        sendBtn.addEventListener('click', send);
    }

    function appendBubble(text, role) {
        const msgs   = document.getElementById('chat-messages');
        const bubble = document.createElement('div');
        bubble.className = 'chat-bubble ' + role;
        bubble.textContent = text;
        msgs.appendChild(bubble);
        msgs.scrollTop = msgs.scrollHeight;
        return bubble;
    }

    function showTyping() {
        const msgs = document.getElementById('chat-messages');
        const el   = document.createElement('div');
        el.id        = 'chat-typing';
        el.className = 'chat-typing';
        el.innerHTML = '<span></span><span></span><span></span>';
        msgs.appendChild(el);
        msgs.scrollTop = msgs.scrollHeight;
    }

    function hideTyping() {
        document.getElementById('chat-typing')?.remove();
    }

    async function send() {
        const input   = document.getElementById('chat-input');
        const sendBtn = document.getElementById('chat-send');
        const text    = input.value.trim();
        if (!text) return;

        input.value = '';
        input.style.height = 'auto';
        sendBtn.disabled = true;

        appendBubble(text, 'user');
        history.push({ role: 'user', content: text });
        if (history.length > 20) history.splice(0, history.length - 20);

        showTyping();

        try {
            const res  = await fetch('/api/chat', {
                method:  'POST',
                headers: {
                    'Content-Type': 'application/json',
                    'X-CSRFToken':  csrf(),
                },
                body: JSON.stringify({ message: text, history: history.slice(-10) }),
            });
            const data = await res.json();
            hideTyping();

            const role = data.ok ? 'bot' : 'error';
            appendBubble(data.message, role);

            if (data.ok) {
                history.push({ role: 'assistant', content: data.message });
                if (history.length > 20) history.splice(0, history.length - 20);
            }
        } catch {
            hideTyping();
            appendBubble('Ошибка соединения. Попробуй ещё раз.', 'error');
        } finally {
            sendBtn.disabled = false;
            input.focus();
        }
    }

    if (document.readyState === 'loading') {
        document.addEventListener('DOMContentLoaded', buildWidget);
    } else {
        buildWidget();
    }
})();
```

---

### Task 5: Подключить виджет в base.html

**Files:**
- Modify: `templates/base.html`

- [ ] **Step 1: Добавить ссылку на CSS в `<head>`**

Найди строку (примерно строка 12):
```html
    <link href="{{ url_for('static', filename='css/animations.css') }}" rel="stylesheet">
```

Добавь после неё:
```html
    {% if current_user.is_authenticated %}
    <link href="{{ url_for('static', filename='css/chat.css') }}" rel="stylesheet">
    {% endif %}
```

- [ ] **Step 2: Добавить JS-скрипт и виджет перед `</body>`**

Найди самую последнюю строку файла `</body>` и добавь перед ней:

```html
{% if current_user.is_authenticated %}
<script src="{{ url_for('static', filename='js/chat.js') }}"></script>
{% endif %}
```

- [ ] **Step 3: Запустить сервер и проверить виджет**

```bash
source venv/bin/activate && python3 app.py
```

Открой http://127.0.0.1:5000 в браузере. Войди в аккаунт. Убедись что:
- В правом нижнем углу есть кнопка 🤖
- Клик открывает/закрывает попап
- Поле ввода работает, Enter отправляет сообщение
- Индикатор загрузки появляется пока ИИ отвечает

- [ ] **Step 4: Протестировать ключевые сценарии**

Напиши в чат поочерёдно и убедись что каждое работает:

1. `привет` — ИИ отвечает на русском, action=none
2. `покажи мой баланс` — ИИ показывает баланс из контекста
3. `добавь расход 300 рублей на кафе` — ИИ добавляет, расход появляется в списке
4. `добавь доход 50000 зарплата` — ИИ добавляет доход
5. `покажи мои последние расходы` — ИИ перечисляет расходы с ID
6. `удали расход [ID из шага 3]` — ИИ удаляет расход
